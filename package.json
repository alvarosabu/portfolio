{
  "name": "@alvarosabu/portfolio",
  "private": true,
  "version": "3.0.0",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=2590",
    "dev:https": "NODE_TLS_REJECT_UNAUTHORIZED=0 nuxt dev --https --port=2591 --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@alvarosabu/eslint-config-vue": "^0.3.1",
    "@nuxt/devtools": "^0.5.5",
    "@types/node": "^20.3.0",
    "nuxt": "^3.5.3"
  },
  "dependencies": {
    "@alvarosabu/ui": "3.0.0-alpha.1"
  }
}
